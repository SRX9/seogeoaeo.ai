# V0.2 — robots.txt / sitemap / llms.txt fetchers + block splitter

- **Status:** ☑ Done
- **Type:** ⚙️ Deterministic
- **Pillar:** ⚪ Foundation
- **Depends on:** [V0.1](v0.1-page-fetcher.md) (types, UA)
- **Unlocks:** V1.1, V1.2, V1.3, V2.1, V2.2

## Goal

Fetch + parse `robots.txt` (per-user-agent rules, sitemaps, AI-crawler status), crawl sitemaps to
discover ≤50 pages, probe for `llms.txt`/`llms-full.txt`, and split a page into heading-bounded
content blocks (consumed by the citability scorer).

## Files to create

- `src/lib/visibility/robots.ts` — `fetchRobots(url): Promise<RobotsResult>`.
- `src/lib/visibility/sitemap.ts` — `crawlSitemap(url, max=50): Promise<string[]>`.
- `src/lib/visibility/llms.ts` — `fetchLlmsTxt(url): Promise<LlmsTxtResult>` (existence + raw text).
- `src/lib/visibility/blocks.ts` — `extractContentBlocks(html): ContentBlock[]`.
- matching `*.test.ts` for each.

## Build steps

1. **robots.ts:** GET `/robots.txt`. Parse line-by-line tracking the current `User-agent`; collect
   `Disallow`/`Allow` per agent and `Sitemap:` lines (fix the `http` split-off bug). For each of
   the 14 AI crawlers, classify: `BLOCKED` (Disallow `/`), `PARTIALLY_BLOCKED`, `ALLOWED`,
   `BLOCKED_BY_WILDCARD`, `ALLOWED_BY_DEFAULT`, `NOT_MENTIONED`, `NO_ROBOTS_TXT`. Return raw text too
   (V1.2 scans it for `Content-Signal:`).
2. **sitemap.ts:** try `/sitemap.xml`, `/sitemap_index.xml`, `/sitemap/`; handle sitemap-index
   recursion (fetch child `<loc>`s); dedupe; cap at `max`.
3. **llms.ts:** GET `/llms.txt` and `/llms-full.txt`; record existence + raw content (validation is
   V1.3).
4. **blocks.ts:** strip `script,style,nav,footer,header,aside`; walk `h1–h6,p,ul,ol,table,blockquote`;
   start a new block at each heading; emit `{heading, content, word_count}`; drop blocks `<20` words.
5. Tests against fixtures: a robots.txt that blocks GPTBot, one with `Disallow: /` wildcard, a
   sitemap index, and an article page (block count + headings).

## 📐 Build from (source of truth)

`inspiration-code/scripts/fetch_page.py`:
- **`fetch_robots_txt()`** (201–306) — the 14-crawler list (206–221) and the full status state
  machine (267–297). Port verbatim.
- **`crawl_sitemap()`** (398–455) — sitemap-index recursion + cap.
- **`fetch_llms_txt()`** (309–332).
- **`extract_content_blocks()`** (335–395) — the heading-bounded splitter + `≥20`-word filter.

## Acceptance

- [ ] All 14 AI crawlers classified correctly on the blocking + wildcard fixtures.
- [ ] Sitemaps (incl. index recursion) discovered and capped at 50.
- [ ] `llms.txt` existence + raw text returned.
- [ ] Block splitter emits correct heading-bounded blocks, drops <20-word ones.
- [ ] Unit tests pass.
