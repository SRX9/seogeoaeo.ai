# V2.3 — Unified audit + composite score (hero feature)

- **Status:** ☐ Not started
- **Type:** ⚙️ Deterministic (aggregation)
- **Pillar:** ⚪ Cross-cutting · Catalog #1
- **Depends on:** [V0.3](../phase-v0-foundation/v0.3-data-model-orchestration.md), [V2.1](v2.1-citability-scorer.md), [V2.2](v2.2-technical-auditor.md) (+ later: V3/V4/V5 analyzers)
- **Unlocks:** V6 (reports/delta), dashboard hero score

## Goal

One audit run → a single **0–100 composite** with six weighted sub-scores, a severity-ranked
findings list, and a 30-day action plan.

## Files to create

- `src/lib/visibility/scoring.ts` — `computeComposite(subScores): { overall, band }`.
- fill in `src/server/visibility/run-audit.ts` Synthesis stage + analyzer registry wiring.
- `src/lib/visibility/action-plan.ts` — group findings into quick wins / 30-day themes.

## Build steps

1. **Composite (port exactly):**
   `overall = citability*0.25 + brand*0.20 + eeat*0.20 + technical*0.15 + schema*0.10 + platform*0.10`.
   Bands: 90–100 Excellent · 75–89 Good · 60–74 Fair · 40–59 Poor · 0–39 Critical.
2. Wire the analyzer registry: V2.1 → citability, V2.2 → technical (others return 0 until their
   phases land — composite still computes, just lower). Mark missing analyzers as "not yet run".
3. **AI-visibility sub-composite** (for the GEO dashboard): citability*0.35 + brand*0.30 +
   crawler*0.25 + llmstxt*0.10.
4. Persist `overall` + 6 sub-scores on the `audits` row; persist findings + per-page rows.
5. **Action plan:** sort findings by severity, split into quick wins (low effort/high impact) +
   weekly themes across 30 days.
6. Tests: composite math on known sub-scores; band boundaries; action-plan grouping.

## 📐 Build from (source of truth)

- `inspiration-code/docs/scoring-methodology.md` — composite **formula + weights** (27–33) and
  score-interpretation bands (53–59).
- `inspiration-code/agents/geo-ai-visibility.md` → **Step 7** (AI-visibility sub-composite weights).
- `inspiration-code/geo/SKILL.md` → Phase 3 Synthesis + action-plan shape.

## Acceptance

- [ ] Composite + bands computed exactly; partial audits still produce a score.
- [ ] Six sub-scores + AI-visibility sub-composite persisted.
- [ ] Findings severity-ranked; 30-day action plan generated.
- [ ] Unit tests pass.
