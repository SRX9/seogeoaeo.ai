# V3.2 — Schema score & sameAs auditor

- **Status:** ☐ Not started
- **Type:** ⚙️ Deterministic
- **Pillar:** 🟢 GEO / 🔵 SEO · Catalog #26 (+ #9 score)
- **Depends on:** [V3.1](v3.1-schema-detector.md)
- **Unlocks:** V2.3 (schema sub-score), V5.1 (entity cross-check)

## Goal

Score schema completeness **0–100** and audit the `sameAs` entity graph across ~14 platforms (the
single most impactful GEO property).

## Files to create

- `src/lib/visibility/schema/score.ts` — `scoreSchema(validated): { score, sameAsAudit, findings }`.
- tests.

## Build steps

1. **Score (port the exact point table):**
   - Organization/LocalBusiness: present 10, with sameAs to 3+ platforms 20.
   - Article/content: present 8, author as Person 12, with dateModified 15.
   - Person (author): present 8, with sameAs 12, with jobTitle+knowsAbout 15.
   - sameAs completeness: 1–2 platforms 5, 3–4 → 10, **5+ incl. Wikipedia → 15**.
   - speakable 10 · BreadcrumbList 5 · WebSite+SearchAction 5 · no deprecated 5 · JSON-LD format 5 ·
     validation (no errors) 5.
2. **sameAs audit:** check linkage to Wikipedia, Wikidata, LinkedIn, YouTube, Crunchbase, X/Twitter,
   GitHub (+ others); mark linked/not + URL validity. This is the GEO entity-resolution signal.
3. Emit findings (e.g., "Add Organization sameAs → Wikipedia + LinkedIn + YouTube") with `fix_payload`
   hooks for V3.3 generation.
4. Tests: a full Organization+sameAs(5, incl. Wikipedia) → high; missing sameAs → flagged.

## 📐 Build from (source of truth)

`inspiration-code/agents/geo-schema.md` → **Step 8 Schema Score table** (244–255, exact points) and
**Step 4b** sameAs platform priority (128–142). Cross-check
`inspiration-code/docs/scoring-methodology.md` → "Structured Data (10%)" (211–222).

## Acceptance

- [ ] Schema score matches the point table exactly.
- [ ] sameAs audit lists each priority platform linked/not + validity.
- [ ] Findings include actionable sameAs gaps with fix hooks.
- [ ] Unit tests pass.
