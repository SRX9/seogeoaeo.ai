# V7.3 — Scheduled re-audits & alerts

- **Status:** ☐ Not started
- **Type:** ⚙️ Deterministic
- **Pillar:** ⚪ Cross-cutting · Catalog (monitoring)
- **Depends on:** [V2.3](../phase-v2-scoring-spine/v2.3-unified-audit.md), [V6.3](../phase-v6-reporting/v6.3-delta-tracker.md)
- **Unlocks:** recurring value, retention

## Goal

Auto re-audit each active site on a schedule, store a new version, and alert on score drops or new
Critical findings.

## Files to create

- `src/server/visibility/cron.ts` — scheduled handler.
- wire into `wrangler.jsonc` cron triggers (reuse content-roadmap Phase 5 cron).
- notification hookup (email/in-app).

## Build steps

1. Cloudflare Cron (monthly, staggered) → `runAudit` per active site → new `run_version`.
2. Run V6.3 `compareAudits(previous, new)`.
3. Alert when overall drops beyond a threshold or a new Critical finding appears (email + in-app).
4. Surface freshness-based refresh candidates (V4.5) as suggested writer tasks.
5. Respect plan caps / active-subscription gating.
6. Test: a simulated score drop triggers an alert; a new audit version is stored.

## 📐 Build from (source of truth)

Orchestration reuses V2.3 + V6.3. Cadence rationale: `inspiration-code/docs/commands-reference.md`
→ "/geo compare" ("run on the first of each month for every active client").

## Acceptance

- [ ] Monthly cron re-audits active sites and stores a new version.
- [ ] Delta computed; drop / new-Critical alerts fire.
- [ ] Refresh candidates surfaced to the writer.
- [ ] Test passes.
