# pnpm: do not auto-install peer deps (matches previous npm legacy-peer-deps=true)
auto-install-peers=false
# Hoist eslint plugins so flat-config can resolve transitive plugins (e.g. eslint-plugin-react-hooks)
public-hoist-pattern[]=*eslint*
